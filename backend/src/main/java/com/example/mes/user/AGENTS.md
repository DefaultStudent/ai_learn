# user 模块

用户模块负责登录会话、角色、用户 CRUD 和用户数据初始化。系统管理员可管理用户并查看系统日志；管理员可查看用户但用户管理页面仅向系统管理员展示。生产/设备管理者和普通用户由方法级权限控制限制到对应部门资源。

认证使用服务器会话和 BCrypt；禁止在前端保存密码或伪造角色。用户变更接口必须经过方法级 `@PreAuthorize`，新增部门审批请求时应使用独立的待审批实体，不能直接绕过权限写入业务表。
