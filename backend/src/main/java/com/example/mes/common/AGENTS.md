# 公共层

公共类的每个构造器和方法都必须有完整 Javadoc。公共参数要使用稳定、语义明确的类型，并为每个参数提供 `@param`，避免调用方只能阅读实现才能理解契约。

日志公共模块使用 Log4j2；关键业务操作通过 `SystemLogService` 入库，普通调试信息只写 Log4j2，不直接访问日志 Repository。

仅放真正跨模块复用的 API 响应、异常、分页、日志与基础配置，避免把某个业务模块的规则塞进 common。
