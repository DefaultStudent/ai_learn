# API 模块

所有 REST 请求通过 `http.ts` 发出；模块 API 文件以资源名命名。请求响应必须定义 TypeScript 类型，错误交由统一拦截器或页面状态处理。

API 边界禁止 `any`。Axios 响应必须使用明确的响应接口和泛型；外部不可信数据先标记为 `unknown`，通过校验函数或类型守卫转换后再交给组件。API 文件不得包含 `__VLS_*` 或任何 Volar 编译产物。
